extends ../../layouts/default.pug
include ../../mixins/box-head.pug 

block main 
    .container.my-3 
        .row 
            .col-12 
                +box-head("Chat")

                .inner-body(my-id=user.id) 
                    each chat in chats
                        div(class=(chat.user_id == user.id ? "inner-outgoing" : "inner-incoming" ))
                            if(chat.user_id != user.id)
                                .inner-name #{chat.infoUser.fullname}
                            .inner-content #{chat.content}
                    //- .inner-incoming 
                    //-     .inner-name A 
                    //-     .inner-content Xin chào 
                    //- .inner-outgoing 
                    //-     .inner-content Hi! 
                    //- .inner-incoming 
                    //-     .inner-name A 
                    //-     .inner-content Bạn có đang bận không
                    //- .inner-outgoing 
                    //-     .inner-content Mình rảnh lắm he he 😀😀😀
                    //- .inner-outgoing 
                    //-     .inner-content Sao nay lại gọi mình vậy
                .inner-foot 
                    form(
                        class="inner-form"
                        action=""
                    )
                        input(
                            type="text"
                            class="inner-input"
                            placeholder="Nhập nội dung..."
                            name="content"
                        )
                        button(
                            class="inner-button"
                            type="submit"
                        ) 
                            i.fa-solid.fa-paper-plane

        